services:
  - type: web
    name: realtime-detail-backend
    env: docker
    plan: free
    dockerfilePath: backend/Dockerfile
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: RUST_LOG
        value: info
      - key: FRONTEND_URL
        sync: false
